# Persistent “Time Since” Timer (Next.js + MongoDB)

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

env

```bash
MONGODB_URI="..."
MONGODB_DB_NAME="timer"
```
